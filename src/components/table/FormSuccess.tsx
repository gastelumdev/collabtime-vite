import { Card, CardBody, Center } from '@chakra-ui/react';

const FormSuccess = () => {
    return (
        <Center mt={'20px'}>
            <Card>
                <CardBody>Form submitted successfully.</CardBody>
            </Card>
        </Center>
    );
};

export default FormSuccess;
